import { MapValuesPipe } from '../../app/map-values.pipe';

describe('MapValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new MapValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
